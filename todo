2. volume for db?
3. README.md things ()
4. save every status if restart server ? (all info in db instead of class ?)

customization
ladder
history

docker  backend + local frontend + firefox works


1. ladder + custom option on randomGame
2. invite friends
3. history (against SQL injection, find querybuilders)


after open two pages for same user, if one log out, the other crash
